// Combinar variables y cadenas en una sola expresión.
let nombre = "Juan";
let apellido = "Pérez";

let nombreCompleto = `${nombre} ${apellido}`;

let mensaje = `Hola, ${nombreCompleto}`;
console.log(mensaje);
