// Notación camel case (camello ó altas y bajas)
let nombreCompleto = "Juan Pérez";

/*

JS es sensibe a mayúsculas y minúsculas, entonces se tiene que escribir exactamente
igual a cuando inicializamos nuestras variables.

--- El nombre de la variable no puede iniciar con un número
--- Se pueden utilizar letras para inicializar una variable, guion bajo, o signo de dólar.

    variable       Variable     _variable       $variable

--- No se pueden usar palabras reservadas en JS, como:

    -abstract       -arguments      - await     -boolean        -byte
    -case       -catch      -delete     -do     -else       -int

*/

let variable = "var Minus";
let Variable = "var Mayus";
let _variable;
let $variable;

console.log(variable);
console.log(Variable);